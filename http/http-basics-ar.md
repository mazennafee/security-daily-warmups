# 🧠 المفاهيم الأساسية للأمن (Key Concepts)

## بروتوكول HTTP عديم الحالة (Stateless Protocol)
- **الأهمية الأمنية:** بما أن الخادم لا "يتذكر" الطلبات السابقة، يجب أن يتضمن كل طلب معلومات الجلسة (Session) للتعرف على المستخدم.  
- يتم ذلك عادةً عبر **الكوكيز (Cookies)** أو **رموز الجلسة (Session Tokens)** المرسلة في ترويسات HTTP (Headers).

---

## أجزاء الاتصال الثلاثة
- **الطلب (Request):** يتكون من الفعل (Method)، المسار (URL Path)، الترويسات (Headers)، والجسم (Body) الاختياري.  
- **الاستجابة (Response):** تتكون من رمز الحالة (Status Code)، الترويسات، والجسم (المحتوى).

---

## الأفعال الشائعة (Common Methods)
- **GET:** يُستخدم لاسترداد البيانات. ⚠️ يجب ألا يغير حالة الخادم أبدًا، وإلا يعرض الموقع لهجمات CSRF.  
- **POST:** يُستخدم لإرسال بيانات جديدة (مثل تسجيل الدخول). البيانات تُرسل عادةً في جسم الطلب.  
- **HEAD / PUT / DELETE:** أفعال إضافية، وغالبًا ما تكون مؤمنة بشكل ضعيف في بعض الـ APIs، مما قد يسمح بحذف أو تعديل الموارد بشكل غير مصرح به (IDOR).

---

## رموز الحالة (Status Codes)

### 4xx (أخطاء العميل)
- **401 Unauthorized:** يتطلب مصادقة (اسم مستخدم + كلمة مرور).  
- **403 Forbidden:** الوصول مرفوض حتى مع المصادقة (تجاوز ضوابط الوصول مطلوب).  
- **404 Not Found:** المورد غير موجود.  

### 5xx (أخطاء الخادم)
- **500 Internal Server Error:** يشير إلى فشل في كود الخادم، وقد يكشف أحيانًا عن مسارات أو تفاصيل تقنية مفيدة للمهاجم.

---

# 🛠️ الأدوات الأساسية للأمن (Commands & Tools)
- **curl -v:** أداة لفحص الاتصال من سطر الأوامر، محاكاة متصفح/تطبيق، وإرسال طلبات GET و POST يدويًا.  
- **nc -lvnp 8080:** فتح منفذ والاستماع للاتصال لرؤية البيانات الأولية (Raw Data).  
- **Burp Suite (Proxy):** الأداة الأهم لاختبار تطبيقات الويب، لاعتراض وتعديل الطلبات والاستجابات في الوقت الحقيقي.

---

# 🔑 تفاصيل هامة للاستغلال (Details for Exploitation)

### الترويسات (Headers) كمصدر للثغرات
- **Host:** قد يُستخدم في هجمات Host Header Attack.  
- **Content-Type:** معالجة غير صحيحة قد تؤدي إلى ثغرات رفع ملفات أو XSS.  
- **Cookie:** تحمل رمز الجلسة. غياب علامات الأمان (HttpOnly, Secure) يسمح بسرقة الجلسة عبر XSS.  

### أمان الكوكيز (Cookie Security Flags)
- **HttpOnly:** يمنع جافاسكريبت من الوصول للكوكي.  
- **Secure:** يضمن إرسال الكوكي فقط عبر HTTPS.  

### جسم الطلب (Body) مقابل سلسلة الاستعلام (Query String)
- **POST (Body):** للبيانات الحساسة مثل كلمات المرور.  
- **GET (Query String):** للبيانات غير الحساسة، لكن يجب اختبارها دائمًا (مثال: `?user=admin`).

---

# 🚀 الملاحظات الأمنية (Security Observations)
- **اختلاف GET/POST:** استخدام GET لتغيير البيانات (مثل `GET /delete_user?id=123`) عيب أمني خطير يتيح هجوم CSRF.  
- **تلاعب الترويسات (Header Manipulation):** تغيير User-Agent أو X-Forwarded-For لاختبار ثقة الخادم بهذه البيانات.  
- **تصنيف رموز الحالة (Status Code Triaging):**  
  - رمز 500 قد يكشف التكنولوجيا المستخدمة (Error-based testing).  
  - رمز 403 بداية اختبار ضوابط الوصول وليس النهاية.
